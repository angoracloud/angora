{
  "name": "@angora/secrets",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Runtime secret loading for the JS/TS services: environment variables, or Infisical when INFISICAL_ENABLED is set",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@angora/config": "workspace:*",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
