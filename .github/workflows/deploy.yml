# Placeholder, intentionally inert: `workflow_dispatch` only runs on a manual
# trigger, never on push or merge.
#
# To activate: replace the TODO steps below with real ones, then change the
# trigger to `on: push: branches: [main]`.
name: Deploy

on:
  workflow_dispatch: {}

jobs:
  deploy:
    name: Build and deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5

      # TODO: build the service images from their existing Dockerfiles, e.g.:
      #   docker build -t angora-backend -f apps/backend/Dockerfile ./apps/backend
      #   docker build -t angora-frontend -f apps/frontend/Dockerfile .
      #   docker build -t angora-slack-bot -f apps/bots/slack/Dockerfile .
      #   docker build -t angora-discord-bot -f apps/bots/discord/Dockerfile .
      #   docker build -t angora-email-bot -f apps/bots/email/Dockerfile .
      - name: Build images (TODO)
        run: echo "TODO - build Docker images"

      # TODO: push to a registry and/or deploy to the real target, e.g.:
      #   - log in to a container registry and push each image
      #   - SSH to the host and run `docker-compose --env-file .env.production up -d --build`
      #   - or trigger a webhook for a managed hosting provider
      - name: Deploy (TODO)
        run: echo "TODO - push images and deploy to the real target"
